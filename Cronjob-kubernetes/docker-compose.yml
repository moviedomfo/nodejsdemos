services:
  olecram:
    container_name: olecram-daemon-ulises
    build:
      context: .
      dockerfile: Dockerfile
    image: moviedomfo/olecram-daemon:1.0.4
    env_file:
      - .env.prod.docker
    volumes:
      - /d/volumes/tallers/logs:/app/logs
